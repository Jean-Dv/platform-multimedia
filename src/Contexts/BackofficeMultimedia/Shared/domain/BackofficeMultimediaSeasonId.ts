import { Uuid } from '@Shared/domain/value-objects/Uuid'

/**
 * Represents the identifier for a Backoffice season.
 * Inherits from the Uuid class, ensuring it is a universally unique identifier.
 * Used to uniquely identify Backoffice seasons within the application.
 */
export class BackofficeMultimediaSeasonId extends Uuid {}
