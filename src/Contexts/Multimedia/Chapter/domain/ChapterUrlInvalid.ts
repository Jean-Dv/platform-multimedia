import { InvalidArgumentError } from '@Shared/domain/value-objects/InvalidArgumentError'

/**
 * Represents a error when a chapter url is invalid.
 */
export class ChapterUrlInvalid extends InvalidArgumentError {}
